# Placeholders batch.*
#    for HSQLDB:
#batch.jdbc.driver=org.hsqldb.jdbcDriver
#batch.jdbc.url=jdbc:hsqldb:mem:testdb;sql.enforce_strict_size=true
## use this one for a separate server process so you can inspect the results
## (or add it to system properties with -D to override at run time).
## batch.jdbc.url=jdbc:hsqldb:hsql://localhost:9005/samples
#batch.jdbc.user=sa
#batch.jdbc.password=
#batch.schema=
#batch.schema.script=classpath:/org/springframework/batch/core/schema-hsqldb.sql

# MySQL
batch.jdbc.driver=${cmt-admin.jdbc.driverClassName}
batch.jdbc.url=${cmt-admin.jdbc.url}
# use this one for a separate server process so you can inspect the results
# (or add it to system properties with -D to override at run time).
# batch.jdbc.url=jdbc:hsqldb:hsql://localhost:9005/samples
batch.jdbc.user=${cmt-admin.jdbc.username}
batch.jdbc.password=${cmt-admin.jdbc.password}
batch.schema=
batch.schema.script=classpath:/org/springframework/batch/core/schema-mysql.sql

jdbc.show.sql=true

invoice_batch.userid=99999999

output.dir=./target
output.file=${output.dir}/invoice.csv

mail.host=localhost
mail.port=10025
mail.to=receiver2@some.com,receiver2@some.com,receiver2@some.com
mail.from=invoice_batch@some.com.invalid

usage.organizationId=10495
usage.periodStart=20130701
usage.periodEnd=20130711
usage.dateFormatPattern=yyyyMMdd
